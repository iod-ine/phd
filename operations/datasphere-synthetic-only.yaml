name: synthetic-only
desc: Train a PointNet++ tree segmentor directly and only on the synthetic forest data.

cmd: python3 -m src.experiments.synthetic_forest_only

env:
  docker: bt1g5fk1421pg7rf6dto

inputs:
  - src/
  - .env

cloud-instance-type: g2.1
